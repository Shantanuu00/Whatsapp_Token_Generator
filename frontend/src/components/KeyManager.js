import React from 'react';
import { Typography, Box } from '@mui/material';

const KeyManager = () => {
  return (
    <Box p={3}>
      <Typography variant="h4" gutterBottom>
        Key Management
      </Typography>
      <Typography>Manage your keys here with enhanced tools.</Typography>
    </Box>
  );
};

export default KeyManager;
